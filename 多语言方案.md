# Penn Notes 多语言方案

> 内部技术方案。实现细节见 [docs/I18N.md](./docs/I18N.md)。

## 1. 背景与目标

站点以简体中文笔记为主，读者中有繁体与英文偏好。目标是先把**站点壳（导航、页脚、搜索、主题控件）**做成可切换的三语，再逐步覆盖正文。

| 语言 | Locale | URL 前缀 |
|------|--------|----------|
| 简体中文 | `zh-CN`（VitePress `root`） | `/` |
| 繁體中文 | `zh-TW` | `/zh-TW/` |
| English | `en`（`lang: en-US`） | `/en/` |

**本期成功标准**：切换语言后 UI 文案正确；同一篇笔记可在三种前缀下打开；简体站路径与行为无回归。

## 2. 范围界定

### 本期（已落地 / 验收范围）

- UI 三语文案（nav / footer / search / notFound / 主题控件等）
- 顶栏语言切换（`LocaleSwitcher`）
- VitePress `locales` + locale 路由前缀
- 正文**不**整站手翻：`en/`、`zh-TW/` 通过符号链接复用简体 Markdown

### 不在本期（后续）

- 全量英文人工翻译
- 业务脚本、RSS、sitemap 的多语言输出
- Decap CMS 后台多语言
- 助手、评论、音乐播放器等全部硬编码清零（部分已接 i18n，剩余见 §7 P4）

## 3. 技术方案

### 3.1 VitePress `locales`

`website/.vitepress/config.ts` 配置 `root` / `zh-TW` / `en`，各自 `themeConfig` 由 `buildThemeConfig(locale, extras)` 生成。

### 3.2 文案表 + 取值 API

| 模块 | 职责 |
|------|------|
| `website/.vitepress/i18n/messages.ts` | 三语文案树；`getUiText(lang)` |
| `website/.vitepress/i18n/types.ts` | `MessageTree` / `UiLocale` 类型 |
| `website/.vitepress/i18n/theme-config.ts` | nav、footer、search、outdateNotice 等 |
| `website/.vitepress/theme/i18n.ts` | `useI18n()`、路径前缀工具 |

- Vue 组件：`useI18n().t('…')`
- 非 Vue（`theme/index.ts` DOM 脚本）：`getUiText(lang)`

### 3.3 语言切换器

- `LocaleSwitcher.vue` 挂在顶栏；路径按 §4 映射切换
- 目标页不存在时回落到该语言首页
- VitePress 自带语言下拉用 CSS 隐藏，避免双控件

### 3.4 正文复用（符号链接）

VitePress `rewrites` 是「源文件 → URL」**一对一**，无法把同一份 `web/foo.md` 同时挂到三个 locale URL。

做法：`ensureLocaleContentAliases(srcDir)` 在 config 加载时于 `en/`、`zh-TW/` 下为栏目目录创建指向 `../{section}` 的相对符号链接；`en/index.md`、`zh-TW/index.md` 为独立落地页，不链接首页。

栏目列表见 `CONTENT_SECTIONS`（`rewrites.ts`）。生成物栏目（如 `books` / `collect` / `news`）在目录存在后才会挂链。

`.gitignore` 忽略 `website/en/*`、`website/zh-TW/*`，仅保留各自 `index.md`，避免把符号链接目录提交进仓库。

## 4. 信息架构与 URL

```
/web/foo          ← 简体（默认）
/zh-TW/web/foo    ← 繁體壳 + 同一份简体正文
/en/web/foo       ← English 壳 + 同一份简体正文
```

| 路径 | 含义 |
|------|------|
| `/` | 简体首页 |
| `/zh-TW/` | 繁體落地页（说明正文仍为简体） |
| `/en/` | English 落地页（同上） |
| `/{section}/…` | 简体栏目正文 |
| `/zh-TW/{section}/…`、`/en/{section}/…` | 同路径正文，UI 跟 locale |

切换逻辑：改写路径的 locale 前缀；若 404 则回首页（`/`、`/zh-TW/`、`/en/`）。

## 5. 已实现清单

对照当前仓库（未臆造）。实现改动与下列文件一并落地：

### 新增

| 路径 | 说明 |
|------|------|
| `website/.vitepress/i18n/messages.ts` | 三语文案 + `getUiText` |
| `website/.vitepress/i18n/types.ts` | 类型 |
| `website/.vitepress/i18n/theme-config.ts` | `buildThemeConfig` |
| `website/.vitepress/i18n/rewrites.ts` | `CONTENT_SECTIONS`、`ensureLocaleContentAliases`、`buildContentRewrites` |
| `website/.vitepress/theme/i18n.ts` | `useI18n` / 路径工具 |
| `website/.vitepress/theme/LocaleSwitcher.vue` | 顶栏切换器 |
| `website/en/index.md` | English 落地页 |
| `website/zh-TW/index.md` | 繁體落地页 |
| `docs/I18N.md` | 实现说明（操作向） |
| `多语言方案.md` | 本方案（决策向） |

### 修改

| 路径 | 说明 |
|------|------|
| `website/.vitepress/config.ts` | `locales` + 启动时挂符号链接 |
| `.gitignore` | 忽略 locale 栏目符号链接，保留落地页 |
| `theme/index.ts` | 注册切换器、DOM 文案走 `getUiText` |
| `theme/css/vars-nav.css` | 隐藏 VP 自带语言下拉等 |
| `ArticleOutdateNotice.vue` / `ArticleSummary.vue` / `Comments.vue` / `RelatedPosts.vue` / `SeriesNav.vue` | 接入 `useI18n` |
| `search-enhance.ts` / `site-runtime.ts` | 文案可随语言切换 |

本地 / CI 加载 config 后会出现（**不提交**）`website/en/{section}`、`website/zh-TW/{section}` 符号链接。

## 6. 限制与风险

| 项 | 说明 |
|----|------|
| 正文语言 | `/en/...`、`/zh-TW/...` 下文章仍是简体；壳本地化，内容未本地化 |
| 文内绝对路径 | Markdown 里写死的 `/about/` 等不会自动加 locale 前缀 |
| 符号链接 | 依赖文件系统；Windows / 部分 CI 需确认 symlink 权限；挂链失败仅 warn，对应 locale 栏目会缺页 |
| 生成物时序 | `news` 等 prebuild 产物目录不存在时不会挂链，需先跑生成再 dev/build |
| 搜索 | 三语 URL 指向同一正文时，搜索索引可能出现重复条目 |
| 未接完的硬编码 | 助手、音乐等仍可能残留中文（见 P4） |

## 7. 后续迭代路线

可按优先级勾选推进：

- [ ] **P1** 繁体正文：构建时用 OpenCC（或同类）将简体 md 转为繁体输出，替代「繁體壳 + 简体正文」
- [ ] **P2** 精选文章人工英译；frontmatter 增加翻译状态（如 `translation: en | pending | none`），列表/详情可展示
- [ ] **P3** 首页 / 关于等关键落地页完善；RSS、`html[lang]`、OG locale 元数据随语言输出
- [ ] **P4** 助手、评论、音乐等剩余硬编码文案清理，统一进 `messages.ts`

## 8. 验收标准

1. 顶栏 **简体 / 繁體 / EN** 可切换；对应 locale 下 nav、footer、搜索占位、404 等 UI 文案正确
2. `/web/foo` ↔ `/zh-TW/web/foo` ↔ `/en/web/foo` 能打开同一篇笔记（挂链成功的前提下）
3. `en/index.md`、`zh-TW/index.md` 落地页可访问，并说明正文策略
4. `npm run build`（或仓库约定的站点构建命令）通过
5. 简体默认路径（无前缀）行为与改前一致，无导航/侧栏回归
6. 提交物不含 `website/en/*`、`website/zh-TW/*` 下栏目符号链接目录（仅 `index.md`）

## 9. 相关文档

- [docs/I18N.md](./docs/I18N.md) — 如何切换、如何加文案、相关文件与实现限制
- VitePress 官方：Multi-Language Sites（`locales` 配置）
